activemq:
  broker-url: tcp://localhost:61616j

spring:
  activemq:
    packages:
      trust-all: true



#spring:
#  activemq.broker-url: vm://embedded?broker.persistent=false,useShutdownHook=false
